## Usage

Build the sandbox:
```terminal
gcc build/sandbox.c
```

Run the build executable

```terminal
./a.out
```

This will create and executable in `sandbox`. Run it like this.

```terminal
cd sandbox
./sandbox
```
