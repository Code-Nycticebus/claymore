#!/bin/bash
# Generated by pybuildc

#----------Variables-----------#
PROJECT="stb_image"
CC="clang"
BUILD_DIR=".build"
CFLAGS=()

#-------------Setup------------#
SCRIPT_DIR=$(dirname "$0")
set -xe
cd $SCRIPT_DIR

BIN=$BUILD_DIR/bin/$PROJECT
mkdir -p $BUILD_DIR/bin
mkdir -p $BUILD_DIR/obj

#---------Compilation---------#
mkdir -p $BUILD_DIR/obj
$CC -Isrc -Wall -Wpedantic -O2 -DNDEBUG ${CFLAGS[@]} src/stb_image.c -o $BUILD_DIR/obj/stb_image.o -c
ar rcs $BUILD_DIR/bin/libstb_image.a $BUILD_DIR/obj/stb_image.o
